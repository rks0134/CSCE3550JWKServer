{
  "name": "csce3550jwkserver",
  "version": "1.0.0",
  "description": "JWT JWKS Server JWT JWKS Server is a Node.js application that serves public keys with unique identifiers (kid) for verifying JSON Web Tokens (JWTs). It also implements key expiry for enhanced security and provides an authentication endpoint for issuing JWTs.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.8",
    "eslint": "^8.49.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.0.1",
    "mocha": "^10.2.0",
    "supertest": "^6.3.3"
  }
}
